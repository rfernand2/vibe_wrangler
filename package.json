{
  "name": "vibe_wrangler",
  "version": "1.0.0",
  "description": "Project + task board that hands work to a coding-agent CLI — Claude Code, OpenAI Codex or Grok Build — each with its own providers and models",
  "main": "server.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rfernand2/vibe_wrangler.git"
  },
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server.js",
    "test": "node --no-warnings=ExperimentalWarning test/smoke.js && node --no-warnings=ExperimentalWarning test/worktree.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
